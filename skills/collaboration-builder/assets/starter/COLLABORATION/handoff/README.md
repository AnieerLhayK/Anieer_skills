# Handoff protocol

A handoff is a self-contained explanation for another role. It points to authoritative source files; it does not replace them.

Create one when a role changes facts another role must use. Include scope, source/version, inputs and outputs, reproduction/validation, limits, recipient and next action. Use `template.md` and `checklist.md`.
