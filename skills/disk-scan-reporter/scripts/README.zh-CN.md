# 脚本

`disk_scan.py` 执行有边界的只读元数据扫描，并生成 Markdown 和带 schema 版本的 JSON 报告，包含分类错误、每个根目录的覆盖率、逻辑大小和分配大小，以及硬链接感知的字节总数。

`audit_guard.py` 静态检查生产脚本中已配置的破坏性操作，将报告写入限制在已批准的根目录，并提供浅层的前/后快照辅助工具。

两者均从技能目录运行，详见 `SKILL.md`。
